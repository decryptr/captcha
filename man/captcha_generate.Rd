% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{captcha_generate}
\alias{captcha_generate}
\title{Generate captcha}
\usage{
captcha_generate(
  write_disk = FALSE,
  path = getwd(),
  chars = c(0:9, letters, LETTERS),
  n_chars = 4,
  n_rows = 60,
  n_cols = 120,
  p_rotate = 0.8,
  p_line = 0.8,
  p_stroke = 0.3,
  p_box = 0.3,
  p_implode = 0.2,
  p_oilpaint = 0,
  p_noise = 0.4,
  p_lat = 0
)
}
\arguments{
\item{write_disk}{write image to disk? Defaults to \code{FALSE}.}

\item{path}{path to save images. Defaults to current directory.}

\item{chars}{which chars to generate. Defaults upper/lower
case letters and numbers}

\item{n_chars}{captcha length. Defaults to 4.}

\item{n_rows, n_cols}{image dimensions. Defaults to 60x120 image.}

\item{p_rotate}{probability to add rotation. Defaults to 80\%.}

\item{p_line}{probability to add strikethrough line. Defaults to 80\%.}

\item{p_stroke}{probability to add stroke color. Defaults to 30\%.}

\item{p_box}{probability to add bounding box to text. Defaults to 30\%.}

\item{p_implode}{probability to add imploding effect. Defaults to 20\%.}

\item{p_oilpaint}{probability to add oilpaint effect. Defaults to 0.}

\item{p_noise}{probability to add random noise to image. Defaults to 40\%.}

\item{p_pat}{probability to add LAT algorithm to image. Defaults to 0.}
}
\value{
list containing two elements: imagemagick object and captcha
value.
}
\description{
Generates random captcha image
}
\examples{

captcha_generate()
captcha_generate(n_chars = 5)

}
